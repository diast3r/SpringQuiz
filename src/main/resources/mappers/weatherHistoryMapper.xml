<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.quiz.weather_history.mapper.WeatherMapper">
	<select id="selectWeather" resultType="com.quiz.weather_history.domain.Weather">
		SELECT
			*
		FROM
			`weather_history`
		ORDER BY 
			`date`;
	</select>
	
	<insert id="insertWeather" parameterType="com.quiz.weather_history.domain.Weather">
		INSERT INTO `weather_history`
		(
			`date`
			,`weather`
			,`temperatures`
			,`precipitation`
			,`microDust`
			,`windSpeed`
			,`createdAt`
			,`updatedAt`
		)
		VALUES
		(
			#{date}
			, #{weather}
			, #{temperatures}
			, #{precipitation}
			, #{microDust}
			, #{windSpeed}
			, NOW()
			, NOW()
		)
	</insert>
</mapper>